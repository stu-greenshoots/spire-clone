# Spire Ascent - Claude Code Project Instructions

**CANONICAL SOURCE: See `/CLAUDE.md` in the project root.**

This file exists for Claude Code's `.claude/` directory detection. All project instructions are maintained in the root `CLAUDE.md` to avoid duplication and drift.

**Do not add instructions here.** Edit the root `CLAUDE.md` instead.
